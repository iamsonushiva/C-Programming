#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n[3],sum=0;
    printf("Enter the three numbers \n");
    for(int i=0;i<3;i++)
    {
        scanf("%d\n",n[i]);
        sum=sum+n[i];
    }
    printf("The sum of %d + %d + %d is %d ",n[0],n[1],n[2],sum);
    return 0;

}
